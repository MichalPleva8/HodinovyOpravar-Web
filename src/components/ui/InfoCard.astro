---
// Info Card Component - Used for contact information cards
interface Props {
	icon: string
	iconAlt: string
	heading: string
	value: string
	href?: string
	class?: string
}

const { icon, iconAlt, heading, value, href, class: className = '' } = Astro.props
const Tag = href ? 'a' : 'div'
const linkClasses = href
	? 'cursor-pointer transition-all duration-300 ease-out hover:translate-y-[-2px]'
	: ''
---

<Tag href={href} class={`flex flex-col items-center text-center sm:flex-row sm:items-center sm:text-left ${linkClasses} ${className}`}>
	<div
		class="icon-container mb-4 flex items-center justify-center overflow-hidden rounded-full sm:mb-0 sm:mr-3 md:mr-7"
		style="background-color: #f5f9fe;"
	>
		<img src={icon} alt={iconAlt} class="icon-image" />
	</div>

	<div class="flex flex-col">
		<h3 class="text-h4 text-neutral-800">{heading}</h3>
		<p class="mt-2 text-xl leading-tight font-medium text-blue-500" set:html={value} />
	</div>
</Tag>

<style>
	/* Base size for mobile */
	.icon-container {
		width: 65px;
		height: 65px;
	}
	.icon-image {
		width: 32px;
		height: 32px;
	}

	/* Full size on tablet and up (640px+) */
	@media (min-width: 640px) {
		.icon-container {
			width: 80px;
			height: 80px;
		}
		.icon-image {
			width: auto;
			height: auto;
		}
	}
</style>
